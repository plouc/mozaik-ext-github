.github
  &__status
    &__current
      height 100%
      width 100%
      display flex
      flex-direction column
      justify-content center
      text-align center

      &__icon
        font-size 8em
        &--ok
          color $success-color
        &--not-ok
          color $failure-color